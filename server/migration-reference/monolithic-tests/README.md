# Original monolithic regression references

These tests document retired direct-account-database and integrated hosting behavior.
They are retained for reference outside pytest testpaths. Active standalone coverage
is in `server/tests/` and `server/space/tests/`, including reservation retries and
native simulation commit fencing. Account-side tests remain in the backend.
